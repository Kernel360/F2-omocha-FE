import { style } from '@vanilla-extract/css';

export const review = style({ display: 'flex', flexDirection: 'column', gap: '30px' });

export const reviewTitle = style({ fontSize: '1.17em' });

export const section = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '15px',
});
