import { style } from '@vanilla-extract/css';

export const basicBid = style({
  display: 'flex',
  flexDirection: 'column',
  listStyle: 'none',
  maxHeight: '400px',
  overflow: 'scroll',
  borderRadius: '8px',
});
